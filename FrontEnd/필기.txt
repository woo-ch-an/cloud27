Java script
1. External JS
2. Internal JS(Script)
3. Inline JS(Script)

1. ~.js를 써서 import 하여 사용
2. <HTML Scirpt ~ 하여 사용하는게 잇음
3. < tag event="script"> 로 작성해서 사용 
>
tag 엄청많음 
1번을 자주 써야 코드 확장이 가능 (자주사용한다) 

브라우저 내부에 객체를 만들어서 객체가 만 들어진다음에 :]



브라우저의 화면 줍기 

뭔말하는건지 모르겠는데 script가 다 읽어지고 나서 실행을 하던 뭘 해야 하는데 
그게 안되가지고 값들이 안들어가는 문제 

-> body 종료 직전에 스크립트 실행하면 됨
가장 쉬운 방법이었습니다

근데 스크립트는 헤더에 있어야 하는데 동작을 안하고
바디에 두면 정석은 아닌데 동작을 하고
-> 헤더에서 뽑아내는 방법이 있음 

1. 화면 지우기 
2. 파일읽고 body에서 메모리 관리시작
3. 다읽으면 렌더링 시작 !
하기직전에 실행시키는 함수가 잇음

      window.onload = function () {};

브라우저가 파일을 모두 읽어서 모든 태그의 DOM 생성을 완료한 이후
 브라우저에 화면을 그리기 직전에 실행

 암튼 자바는 External로 작성하도록 하자

변수다루는게 아주 쉽다

if(condition) 에 들어가는 변수명(변수명만들어갈 때)
값이 있냐 없냐로 판단함
가치판단을 한다 (주제에)
숫자인데 0이다 -> 없다고봄
문자열인데 공백임 -> 없다고봄 (space 말고 blank대)
Undefined -> 걍 대놓고 없음 
Null -> 없는거

같다 판별 : == or === 
뜻 : 
== -> 값만 비교 
=== -> 값과 타입을 비교

**** JavaScript 배열

push 배열의 마지막에 추가
unshift 배열의 처음에 추가

shift 배열의 처음에서 제거
pop 배열의 마지막에서 제거

(특질계) splice(index, count);
index번호부터 count 갯수 제거 

앵간하면 JAVA와 같다

argumentsf 라는 읽기전용 클래스 덕분에 파라미터가 넘치던 모자라던 일ㄷ들똧았다 !


ㅎㅇㅎㅇ 0319 
중첩함수 
-< 쓰는이유 : 함수 감출라공

중첩함수는 밖에서 알수가 없음
자바처럼 함수의 존재는 {} 범위 안에 있다는것 

callback
주로 비동기 또는 이벤트 핸들링에서 사용하는 함수의 표현식
비동기 특) 
어떤 함수가 시작하는 시간 또는 지점  아님 종료되는 시간 또는 지점이 명확하지 않은 코드의 형태
정상적인 코드 흐름에서 분리된 형태
---->>>
하나의 명령이 종료되지 않은 상태에서 다음 코드가 실행된다


비동기함수가 끝나면 파라미터로 전달한 함수가 실행되도록 해서 함수가 끝남을 확인 
마치 window.onload 
C#에서도 비슷한거 있었는데 끝나면 뭐 하는 그런게 

자바스크립트 -> 이벤트 대응을 위한 언어 뭐 그런거라
앵간하면 콜백임

이름 없고 함수가 파라미터면 콜백

  lizard.addEventListener("click", function () {
    console.log("Lizzard 🦎");
  }); 

  콜백임 

이벤트로 내용 바꾸기 
-> 변수(태그).innertext = "content";
-> 변수(태그).innerHTML = "<h1>content</h1>"; 
하면 태그도 사용 가능함

변수에 함수도 들어가는데 이름을 똑같이 주고 함수이름을 지우면 쓰기 편함

변수명=함수명이 되어버린다
  var printMessage = function (message) {
    console.log(message);
  } 
  printMessage("asd");
를 함수표현식이라고 부름
함수표현식을 이용하여 콜백을 전달 

비동기 함수 만들기

    setTimeout(function, delayTime);
run the function after delayTime;
delayTime = ms


  function printSumResult(from, to, endfunction) {
    setTimeout(function () {
      var sum = 0;
      for (var i = from; i <= to; i++) {
        sum += i;
      }
      console.log(sum);

      endfunction(sum);
    }, 0);

  }

  printSumResult(1, 100000000, function (sum) {
    alert("asd!!" + sum);
  });

  이거 개신기하네 ㅋㅋ

New attribute unlocked;
-> data-변수명="초기값";

    <ul class="list" data-count="10">
example


즉시실행함수
워딩 개쩐다
-> 함수생성하자마자 스스로를 실행시키는 함수
뭔말인가여 
: 원래 함수 만들고 다른데서 호출해서 쓰는데 얘는 아니란뜻입니다

(함수)(); 
  (function () {
    console.log('나를실행해봐');
  })();
이게되네 ㅋㅋ

바닐라 스크립트에서 변수의 범위 : function
if절같은거의 중괄호랑은 하등상관업다 

호이스팅 hoisting
 -> 변수 지 좃대로 정의함 (변수의 정의를 끌어올림)
[hoister : 뭔가를 위로 들어올림]

함수를 리턴하는 함수 
거꾸로 해도 f(x) the return f(x)

내가 가진 인자랑 뭐랑 다 합쳐서 함수로 던져준다
ㅡㅡ 까지가 함수

***** 객체 리터럴 ***** [객체 표현식]
원하는 기능 속성 다 때려박기 가능  

  var newObject = {};

  newObject.name = "demitrian";
  newObject.title = "titus"

or 
var newObject = {
    namae: "demetrian",
    "ti-tle": "titus",
    powerOn: function () { console.log("키기") },
    powerOff: function () { console.log("끄기") },
  };
(함수도가능함)

영어말고 특수기호가 필요할떈 ""로 감싸서 사용

상속에 대하여 : 

String의 기능이 뭐가 있나 ?? -> contains 기능 없다 
만들어야징

      String.prototype.contains = function (findtext) {
      console.log(findtext);
      console.log(this);

      return this.indexOf(findtext)>=0;
      };

prototype : 원본에 추가하여라 ~
자바 스크립트의 상속
text.replaceall 실행하면 
~ String -> 한테가서 있나봄
 없넹 -> prototype -> 한테가서 찾아봄 어 있네 
 하고 사용
* 근데 쟤도 없으면 prototype의 prototype한테감
이때 prototype의 부모는 String이 아닌 Object임

부모한테 가면서 있나없나 계속 찾아보고
없으면 에러 터짐 (Uncaught TypeError)
정보) 함수는 추가/수정이 가능하다 
기존껏도 수정 됨 오버라이딩이 기본임

jQuery 조금만알아도 금방쓸 수 있어서 잘씀
(느림 편함 ECMAScript 지원안함)
26년 초 마참내 ECMA Online 


ㅎㅇㅎㅇ 0320
제이쿠ㅓ리 등장
* window.jQuery인ㄷㅔ 윈도우 생략 가능
** jQuery 를 $로 대체 가능함

      window.onload = function () { 
      var h1 = document.querySelector("h1"); 
      console.log(h1.innerText); 
      h1.innerText = "What do you want to do?"
      }; 
-> 이런 내용이
      jQuery(document).ready(function () { 
            var h1 = $("h1");
            console.log(h1.text());
            h1.text("What do you want to do");
      });

-> 
      $(document).ready(function () { 
            var h1 = $("h1");
            console.log(h1.text());
            h1.text("What do you want to do");
      });
-> 
      $().ready(function () {
            var h1 = $("h1");
            console.log(h1.text());
            h1.text("What do you want to do");
      });
->
      $(function () { 
            console.log($("h1").text());
            $("h1").text("What do you want to do");
      });

이렇게 줄어듬 ;


window.onload = function () {
    var listItems = document.querySelectorAll("li");
    console.log(listItems);
    for (var i = 0; i < listItems.length; i++) {
        listItems[i].innerText = "Seoul";
    }
};
-> $(function () {
    $("li").text("Seoul");
})

??

pseudo Classes 
정보) JQuery 는 셀렉팅이 핵심이고 

    $("#destinations > li:nth-child(2)").text("Gyoung ju"); 
    CSS랑 똑같이 사용 가능함

검색이 좀 느림
(css 셀렉 자체가 느려서 )
를 대체하기 위한 

DOM Traversing
좀 작성할게 많지만 월등하게 빠르게 찾아오기 가능
쉽게보는 예제
$("li:fisrt-child");
$("li").first();
에서 아래 방식이 더욱 빠르다

first last next prev 같은게 있는데 별로 안쓰인다

많이쓰이는거 : ~의 부모에게 ~적용(찾기)
parent(); 를 사용ㅎㅏㅁ 2
랑 자식찾기
children();

근데 부모.find(자식) 이랑 부모.children();이랑 뭐가 다를까
find(자식) -> desandeant 
children(태그) -> 직속 자식만 


일반 객체들은 Jquery함수 적용이 안되기 떄문에
앵간하면 객체 셀렉할 때 $() 로 감싸서 쿼리로 사용할수 있도록 하자

굉장히 쉬운뭐 그렇다고 볼 수 있ㄱ겠습니다 ~

DOM 다루기.

before -> 형제중 형이됨
after -> 형제중 막내가 됨
prepend -> 젤 앞에 붙인다 (첫째 자식이 되고) 
append -> 젤 아래 붙인다 (막내가 됨)

append는 많이 사용함

* 정보 remove() 는 dom tree에서 완전 제거라
이후 조회에 대해서는 암것도 안댐
detach -> dom tree는 제거 안하고 화면에서만 빼는데 안씀

closest() 기준으로부터 가장 인접한 부모를 찾아감


ㅎㅇㅎㅇ 0323 
vanila에 remove child 됨 
blcok -- element __ modifier 
블록 __  내부요소  -- 기능
장점 : 추적 용이 
단점 : 남발
적당한곳에서 사용
오후 -- 금방끝나나 
callback = "할당" / $().ready (함수)
는 window.onload에 할당이 될 것 / 동작하는것이 아님

window.onload 에는 동적 생성된 태그에 대해 이벤트를 할당할 수 없다

-> 클릭 이벤트에 추가하거나 다른 방식으로 사용해야함 ~

 첨에 존재했던 DOM(button-area)를 통해 새롭게 생성된 p.whitecolor에게 ㅔClick이벤트 주기
    $(".package-button-area").on("click", "p.white-color", function () {
        alert($(this).text())

    });

근데 클릭이벤트에 추가하는게 더욱 정석적인 방법이며 유지보수에 용이함
클릭이벤트에 준다는 뜻: 객체를 만들 때 이벤트를 주기

mouse event -> click mouseover mouseout mouseenter mouseleave
mousedown버튼 누르고 hold, 띄우기 up , move 드래그
가 가장많이 사용됨
over enter | out leave < 머가다른가욤
jquey 참고
over 올라가있을때마다 실행 
enter 입장할떄만 실행 

<over> 나가는 순간에도 동작/자식에게 올라가도 동작
너무 빈번한 이벤트를 발생시켜서 
        $("#amount").text(count * 399.99);이상한거잘만듬 
위험함 ~ 조심

on 연결해서 사용 가능 다른 속성도 가능

부모의 스타일을 바꾸고 싶음 --> css에서 안된다 

keyboard 이벤트들 
keypress / 누른채로 유지 , keydown / 누르기, keyup / 떼기 

사용자 입력값이 필요할 ㄸ때 : 키다운 
사용자 입력값 검증 할 때 : 키 업

AJAX : 요즘 안씀 
-> API -> Open API -> RestfulAPI 다 같은말 이긴함
비동기 정보요청

일반적인 http request .
1. 브라우저의 리퀘스트 
2. 서버의 응답
[동기방식]
브라우저 : 뭐하다가 리퀘스트 발생하면 멈추고 요청 [대기]
컨텐츠 싹 삭제 후 리스폰받은 html으로 채우기(html을 받은거면 아님뭐다른걸로채우고 그렇습니다.)

1. 뭐 누르면 싹 삭제함 
2. 뭐 하는동안 싹다 막음
이 불편해서 비동기식 방법으로 만든 AJAX
브라우저 가만있고 JS가 요청을 보내기
JS -> URL 전송 [대기] -> return 값 callback
그동안 브라우저는 삭제하거나 동기대기를 하지 않는다

근데 그새 또 진화해서 fetch 라는게 생김
-> 프로미스라는것을 알ㄹ고있어야한다
작업이 완료되지 않았는데 뭔가 반환될수 있다는 것
-> "비동기화" | 

비 동기적으로 완료된 작업에 대해 결과를 처리할 수 있어야 한다

  setTimeout(function () {
      try {
          if (delay % 2 === 0) {
              throw new Error("Error inquetuin");
          }
      }
      catch (e) {
          console.log(e.message);
      }
      console.log(delay);
  }, delay);

Promise
prom을 만들고 then 이 성공
catch는 실패
-> Try Catch 를 좀 보기 쉽게 ?? 비동기 코드를 동기로 만듬
중첩비동기문도 가능
.then .then .ten  .... 으로 이어지는 등 캐치도 가넝함 

-> 근데 프로미스 직접 안만듬

json
역할 : 국제표준 뭐 그런거로 html request 언어
reception 은 영어가 기본이다 뭐 그런 느낌 .. 

* 일정 주기로 함 수 반복;
setInterval(function() {} , 1000); 1초주기로 계속반복
setInterval(function() {
    console.log(fechProm);
}, 100); 


Quiz
> window.onload??
*** var 안붙이면 어디로 가나 window가 붙어서 다른데로감   