태그 Element, Node라고도 표현 (위치에 따라)

HTML은 다양한 형태의 데이터를 표현하기 위해 태그를 미리 정의해 두었다
(사용자 추가 불가능 )

태그 <tag> = OpenTag
     </tag> = CloseTag
태그 안에 다른 태그를 넣을 수 있다

태그 종류는 무지하게 많기 때문에 
w3school 에서 확인하기 빨간줄 = html5 에 없음 (4까지 지원)
p.13 예시
부모태그 : 태그명이 사용 될 수 있는 태그범위
title은 반드시 head안에서 사용 될 수 있다

form -> 안에 있는 값을 BackEnd에게 전송할 때 사용
시멘틱 웹 태그  -> <header, aside, footer 덩덩 >  의미대로 사용할거 아니면 의미 없음
-> 보통 퍼블리셔라는 사람들이 규약을 뿌릴 때 정리됨

속성 
 태그에게 줄 수 있는 별도의 값
 tabindex -> tab으로 선택되는 순번 정하기
 속성중에서는 class를 가장 많이 사용함 

이벤트
 tag 속성 이벤트명 = JavaScript 코드이름 
  -> 이벤트가 발생하면 JavaScript를 실행할 수 있도록 함
 onkeydown, onclick 만 알면 나머지는 쓰다보면 외워진다

HTML 끝
 웹 페이지를 구성하는 방식 
 어떤 영역에서 뭘 보여줄지만 정한 꾸며지지 않은 건조한 상태
 가끔 인터넷 반만 터지면 볼 수 있음 

CSS Cascading Style Sheets
어렵다.
 부모태그와 자식 태그의 스타일을 동일하게 적용된다 (Cascading, 계단식인 이유)
HTML 을 꾸밀 수 있따
 
ㅎㅇㅎㅇ 2026 03 09 

ID Selector
#태그명 {} 
-> 태그명 id =>> div id= 태그 로 구분 
#header ,,, <div id = header> 

Class selectors
.header--logo ,,, <p class = "header--logo"> 

Element Selectors 
P{}
div {}
-> p , div 태그 전체

Universial Selector
* {} -> 걍 다

아마 Class 를 가장 많이 사용하게 될 것

웹 사이트 전체 레이아웃을 적용하는 External Style 
한 페이지의 레이아웃만 적용하는 Internal Style
한 태그의 레이아웃만 적용하는 Inline Style

External -> HTML 안에 <style> 태그로 사용 
Internal <h1 style = "color:blue"> ~ --> 태그 추적이 어렵기 떄문에 피해야 한다

** color 설정에서 RGB뒤에 2byte 더 추가 가능한데 이는 투명도를 뜻함 
color : #44554488 <-- R 44, G 55, B 44, 투명도 88

css 임포트 하는 방법
<link rel="stylesheet" type="text/css" href="상대위치">

스타일별 우선순위 
1. Inline  Style
2. Internal Style
3. External Style
< 작성 순서에 따라도 우선순위가 나뉘어진다

Cascading, Spectificity
같은 태그에 여러개의 스타일을 정의할 수 있다 -> Cascading
스타일 충돌이 일어날 경우 명시도에 따라 처리된다 -> Spectificity
ㄴ 순서가 존재한다

동일한 selector를 여러번 사용 할 경우, css가 작성된 순서대로 브라우저가 표시한다

Inline으로 css작성시 명시도의 우선순위는 의미 없이 Inline으로작성된 css가 먼저 인식된다 
스타일은 복잡하게 되면 페이지가 느리게 동작함

Box Model Styling
p.54 
* 박스 크기는 Border 까지고 Margin은 박스 외부의 넓이로 다른 측정값을가진다
Border Box를 연습해봅시다

* color 지정할 때 ff00ff -> 이런거 간단하게 f0f 로도 축약해서 사용이가능함

Margin Collapsing -> 중첩된 마진에 대해서는 중첩을 시켜 큰 마진으로 덮어씌운다
-> 외용 나도몰라 개미친년아

폭과 넓이  Width & Height
-> 기준은 항상 부모 Element의 영역안에서만 가능하다
 **** 크기는 항상 부모의 영향을 받는다 

 Top & Bot 과 Rig & Lef 의 값이 쌍으로 같으면 20 10 20 10 -> 20 10; 으로 축약 가능

 * 어떤 box는 아래로 정렬되고 어떤 box는 우측으로 정렬됨
 외용 ? -> Display값이 그래요
 아래로 떨어지는 얘들 -> display:block; 이라는게 있었다 
 옆으로 가는 얘들 -> 없었다
 
 block 뜻 : Width가 남든 어쩌든 걍 한 칸 먹음 (남은 부분은 Margin)
 block에 아무것도 안주면 inline Display가 먹음(옆으로 가는 얘들)

 Inline Display 
 부모 Element가 자식 Content의 길이보다 클 경우 -> 한 줄에 표시 가능
 안되면 마지막 content부터 아래로 떨어짐
 Element 가 컨텐츠 영역만큼 의 크기를 가진다
* Width와 Height는 부여 불가능 
줘봤자 브라우저에서 쓸모없는데숑 이 출력됨 
-> img는 예외 (inline Element Tag지만 실제 동작은 inline block Element처럼 동작)

1000px 을 500px 500px 딱 나눈다면 둘 다 표시되는게 아님(공백 추가)
1001px이면 된다-> 드래그 해보면 뭔가 있음 


260310 ㅎㅇㅎㅇ
코드리뷰 -> 태그사용에 있어서 태그들은 명확히 구분된 역할을 수행해야한다
p 태그 -> 단락구분용

Verticla -> list 사용


event 로 넘어가기
a:hover, a:active 둘 다 설정시 둘 다 적용 가능함
근데 active가 먼저 나오면 뒤의 hover만 적용되어 
** 우선순위때문 

(복습용)
1. Inline  Style
2. Internal Style
3. External Style
< 작성 순서에 따라도 우선순위가 나뉘어진다

의사 요소 
backdrop, after, before 를 가장 많이 사용하게 된다
대화창 + 단락 앞에 문자추가 + 뒤에 추가 등등

important -> 레벨 격상시켜줌 
근데 important 또한 중복 시 
기존의 우선순위로 사용된다

float 사용시 display-block 해제됨
옛날에 float를 이용해서 아이콘 등을 배치했다
(지금 -> 아님)
ㄴ 순서대로 배치되기 떄문에 순서에 유의 

outline -> border  아님  !! 

Absoult의 기준 -> Viewport
Relative 의 기준 -> 나 
정확하겐 off set기준
* Relative의  부모 Element는 기존의 모습을 유지한다 

offset 기준이 두 가지.                         [offset 기준]
   1. position이 할당되지 않은 부모 태그 내부 ==>  View port
   2. position이 relative 로 할당된 부모 태그 내부 ==> 부모태그 


ㅎㅇㅎㅇ 0311 
w3school 홈페이지 분석 & 설계
마지막 여백은 유동적으로 움직이는 것을 보니 Margin으로 사용 
좌측은 스크롤이 별도로 존재하며 마지막 footer부분에게 먹힘
맨 위 List 들로 float 좌 우 로 놓은 듯 하다.
(이름을 네비게이터라고 부른다 )

HTML + CSS 의 실력 향상 방법 : clone
clone 이란 ? (지금 하는거)
* {
  box-sizing: border-box;
} << 모든 박스 사이즈를 맞춰놓음으로 작업하기 편리하게 함 


0312 ㅎㅇㅎㅇ

z-index 
 엘리먼트 위에 다른 엘리먼트를 덮어 씌운다
 absolut, fixed 등에 적용 가능

overflow 
 자식 엘리먼트의 크기가 부모 엘리먼트의 크기를 넘어설 때
 흘러넘치는 현상
 에 대한 대처법
 scroll -> 스크롤 바를 이용해 자식 내용을 보여줌
 hidden -> 걍 안보이게 하는거드래그해서 보이게할 수 있는 등 
 clip -> 짜르기  뭐 이런게 있스빈다
 -> 글자 쭉 쓸 때 중간에 공백이 있다면 다음줄로 넘어가게 되는데 이 기능 안쓰려면 white-space -> nowrap 을 주면 됨 (스타일에서)
 nowrap이 있어야 overflow나 text-overflow가 사용 가능 왜냐면 한 줄이어야 되니깐 끄덕

 백그라운드 background
  -> table ?? 
갑자기 tabel
 -> 사용법은 알아둬야 좋을것같슴다

모든 값에 값을 넣는다면 비율로 움직이지만 (뒤에 단위랑 관련 x )
한 두개가 빠져있다면 고정 값으로 움직일 수 있다

thead는 보통 배경색만 준다 
padding도 안먹기때문 -> inline 으로 되어있다 
(table-header-group이긴한데 inline으로처리)

테이블 간에 칸들끼리 spacing 들이 있다(기본적으로)
-> 외요 ? 칸 구분하려고용
없애는법 : table style에 border-collapse : collapse;
div 말고 table 을 사용하는 이유 ?
-> 병합기능이 강력하기 때문

background
 -> 다시돌아옴 
 backgroundImg는 왜 쓰는걸까 
 그냥 img 쓰면 되는데
 -> 브라우저가 데이터를 들여오는 과정떔에 그럼
 --> 마리오 배경화면처럼 몇개 해놓고 거기서 어디를 쓴다로 하는 것
 이미지 하나에서 부분의 픽셀 만 가져와서 쓸게용

size & unit
 사용자 친화적 웹 개발을 위해 필수 
 position : fixed에서 %는 걍 무조건 ViewPort로 감
 (부모 element가 있어도 그냥 viewprot로 뜀)

position : absolut 내에서의 %
부모의 element의 크기를 따라간다
position : static , Relative 에서 :
똑같이 부모 꺼 따라감 

* 부모 없으면 Viewport로 간다

height 100% 먹이는 방법 : html의 height를 100%로 한다
최소/최대 width:지정 가능 -> 크기가 변하다 최대/최소  width가 되면 더이상 변하지 않는다 
height도 같은 효과가 있음

> 옆에 대시보드(사이드메뉴)가 줄어들다가 사라지는거 :
반응형 웹의 시작

rem : 사용자 지정 폰트 크기 의 배수
-> 사용자 지정 16px  = 1rem
-> 2rem = 32px 
-> 10px = 0.625rem 등등 계산기와 함께한다
em : 부모의 단위 따라감
부모 : 32px = 1em
내꺼 16px = 0.5em
>> em은 잘 안쓰고 rem만 주구장창쓰게됨

페이지 검사에서 검사기 => 계산됨 가보면 계산되어 있는 rem 값이 나와있음 참고용s

이거는 폰트 사이즈 뿐만 아니라 길이나 패딩 같은 px단위 쓰던 모든 곳에 적용이 가능함
-> px단위는 안쓰는게 좋다
width : 50%; 보다는 width : 50vw; 
(viewprot width라는 뜻)
hight는 viewprot height -> 50vh;

미세먼지팁 : 값 잘못줘서 maxValue < minValue 일 때 
minValue가 이긴다


ㅎㅇㅎㅇ 0313 
코드 리뷰 : 디센던트 
-> css에서 .클래스명 , .클래스명{
}
이 아니라 
.클래스명 .클래스명 {일 때 }
디센던트라고 부름 
이게먼데용 -> 
같은 부모를 둔 엘리먼트들을 형제 라고 부를 수 있따
부모 
  ㄴ 자식(형제)
  ㄴ 자식(형제)
  ㄴ 자식(형제)
란것 이를 돔 트리라고 부를 수 있겠습니다 
부모 > 자식 처럼 표현도 가능하고요.
또는 부모 > 자식 > 자식 으로 표현도 가능했구요
근데 부모 손자 로 표현한다면 중간에 있는 모든 자식을 확인하면서 찾아가게 됨
이를 디센던트라고 합니다 
알아보기 좀 어려워지지만 코드 재활용도 높아짐 뭐 그래요
& : 상위 
뭔말인가여 -> 
.부모{
  & > 자식{
    & > 이렇게 들어가면서 코드 작성 가능
  }
}
이렇게 묶어서 다님 코드의 재활용도 높아짐 뭐 그래요 2
근데 코드 길어지면 관리가 어려워짐

보스 - 반응형 웹 디자인
서마트폰 출시 당시 네이버에 접속하면  개~ 한 화면으로는 네이버 화면을 주기 어려웠음
그래서 핸드폰화면크기에 맞는 화면을 제공
www.naver.com -> www.m.naver.com 탄생
근데 시대가 지나면서 핸드폰의 화면은 여러개 탄생 4 5 6 7 8 인치 등등등 + 태블릿도있음
그런 격변의 시대에 HTML5 등장 , @media 가 추가된 모델
 @media = 사용자의 화면에 따라 레이아웃을 다르게 줄 수 있게됨 
 [맨날 html set 하면 삭제하는 그 줄 말하는거임. ]
 * 그래서 m.naver 삭제했다가 작업량이슈로 다시 복각됨

https://yesviz.com/ < 모든 기기 해상도

 @media -> 에서 저거 불러오기 가능 = media query 
이를 이용해 창 줄어들면 사이드바 없어지거나 하는 거가 가능하게 된다

.wrapper {
  width: 100%;
  max-width: 1400px;
  min-width: 900px;
  margin: 0 auto;
  font-size: 0 rem;
} < 이거좋은듯 
ul.h-list>li.item*3 < 더좋은듯

flex box
- > 중 요 함 제일 근데 별거없음

flexbox = display container system
박스들을 표현하는 박스 (유연함)
아이템들을 한 방향으로 배치시킬때 유용한 방법
[??]
아 전에 하던건 fixed, 지금 하는건 flex
flex 특) 겹칠라고 공백도 겹침
flex에게는 공백=들어올수있는공간 으로 인식

*row <- default value
flex-direction : row (->) row-reverse(<-)
flex-direction : column (down) column-reverse(up)
메인 축 : 아이템들 배치 방향 (div)

css 없이 flex처럼 표현하기 위해선 java 도 필요할정도로 강력한 정렬방식

교차축 정렬이란 메인축을 기준으로 정렬을 시도
할게많은데 어캐 아나요 ->
코드로하는게 아니라 브라우저에서 해보는거임
(justify-content)
마찬가지로 align-item도 가능

->> 지금까진 박스에 드간 박스 정렬 

이제는 그 안에 들어있는 아이템에 대한 정렬 
flex item 
-> 브라우저 치트키 못씀
1] flex basis
2] flex grow
3] flex shrink
메인 축에 영향을 받는다

1. flex basis = 메인축이 가로면 width, 세로면 height
ex) flex-basis : auto; = 컨텐츠 크기만큼 채우셔
그외 숫자 = 최소 길이로 적용 (자기보다 큰 item오면 그거 따라감)

2. flex grow = 1 이면 남은 여백을 다 채운다
( 여백을 균등 분할해서 아이템들에게 뿌림 )
사회주의적 태그이므로 사용을 지양해야겠네요
'grow가 1인 얘들한테만' 뿌림

3. flex shrink = flex basis보다 작아지게 되면 shrink 에 할당한 비율의 값 만큼 줄여라 ? 
{grow는 할당된값만큼 많이 받아옴  (+값을)
 shrink는 할당된 값만큼 많이 받아옴 (-값을)}

flex: grow shrink basis ; 로 한번에 할당도 가능

>> FLEX 사용처  ::  어디요 ?


ㅎㅇㅎㅇ 0316

1. 헤더부분 학사모랑 몽키스패너는 ? 

2. 화면 배치 뭘로 어캐했나욤 다 플렉스인가욤

3. 섹션 머임
> div 
url encoder

a href="#" 뜻 : 내 위치로 이동
다른 위치 보내기 : 
a href="#main"
-> 누르면 id 찾아서 감 (북마크기능)

<a id="main"> </a>

line-height -> 행간 늘리기

flex vs grid
한쪽 방향으로 배치 vs 양쪽으로 배치 
grid는 병합도 가능하다 Table과 비슷함
그럼 Table쓰지 왜용 -> Table보다 섬세한 조절이 가능

felx의 grow와 비슷한 fr -> 남는값 나눠주기 

*minmax(min, max) 사이에 값으로 알아서 지정하게 되는데 뒤에 값이 px등으로 고정값 주면 (auto 이외)
그 값으로 고정댐
자주 사용됨

grid 합병할떄 팁) 칸으로 재는게 아니라 줄 번호로 잰다고 생각하면 편하다 n번째 줄부터 m번 줄까지 ~ 
줄번호 역순으로도 가능 (1/-1) <- 끝까지라는 뜻


ㅎㅇㅎㅇ 0317
셀렉터 
div class="grid"
table class="grid"

.grid{
  이면 둘 다 적용되지만
}
div.grid{
  면 div뒤 grid만 적용
}
table.grid{
  같은 이름인데 다른 스타일을 주고 싶을 때 사용
}

폰트 스타일링 Font styling
내가 원하는 폰트로 서비스를 제공하려면 들어오는 고객이 해당 폰트를 이미 구매해서 깔아놓고 와야할까 ? 
-> 아니용

서버에 저장해놓고 그 폰트를 제공 / ???: 저는 이거 싫은데요-> 대처x
Web Font를 이용해서 제공 가능함
https://fonts.google.com/ < check it 
원하는 폰트를 찾아서 좋은(Open) 라이센스면 ! 사용 가능 한데 변호사도 애매해서 한발뺴는 영역
-> 시 군 구에서 내는 폰트는 진짜 무료니까 안전하게 사용가능
다람쥐 헌 쳇바퀴에 타고파 -> 모든 모음이 있다 


--> 구글 저기서 원하는 폰트 찾아서 사용하기 

form styleing -> 레이블 박스 뭐 그런거죠

div p {
  div 아래 모든 p
}

div > p{
  친자식 p
}
div+p{
  형제 p 
}

div ~ p{
  div 뒤  태그들
}
중요한 얘들임
더 중요한 input

라디오버튼 그룹 만드는법 :
이름을 같게 설정해준다(name)
** appearance : auto이기 때문에 
패딩 백그라운드 컬러 뭐 그런거 안됨 
마진이랑 길이 높이는 지정 가능 
이미 표현방법이 정해져있기 때문에 바꿀수는 없다 
꾸미려고해도 못꾸밈 -> 개꿀
꾸미려고 시도하면 선택되어있는지 아닌지 못봄
대신 Label을 꾸미게됨


input[type="radio"] + label{ 
}

<< 개어려운영어 (근처)

select items 꾸미기 굉장히 어려움 (검색하면서 찾아봐야 함))

ㅎㅇㅎㅇ 0318 

Transform
 - 2D변형 가능 
 - 3D도 가능
 > x,y 에 지정된 매개변수에 따라 요소를 현재 위치에서 이동시킴
 > 함수로 위치이동
 Scale - 크기변경인데 반전가능 (뒤집기)
 Rotate - 돌리기 가능 등등 단위 : 45deg=0.125trun 이런식
 굉장히 많음
Doom도 만듬

--> 결국 위치이동인건데 absolut랑 뭐가 다른가용
: 애니메이션을 위한 움직임이라고 보면 됨

애니메이션 : transition 
-> 디테일하게 지정도 가능함

transistion은 자기 자신에 대한 변화에서만 적용 
자식이 변한다고 transistion을 해도 동작에는 영향을 주지 못함

애니메이션 다 떄려 박으면 성능저하의 원인이됨 
JavaScript ---- START
브라우저에서 동작하는 CSL (Client Side Language)
범용이라 서버에서도 사용 가능

이벤트 기반의 동작 언어 
VScode도 이걸로만듬

Compiler 언어가 아님
기존) SourceCode -> Compile -> Machine
JS -> SOurceCode -> Machine

변수의 타입이 정해져있고 크기도 지정해줘야 함
그런거 없이 그냥 쓰는게 Interpreter
한줄한줄 컴파일해서 동작하기 때문에 느림

*단점) 문법적인 오류 검출 불가넝 
 -> ESLint라는 툴로 최소한의 문법 오류 검출 

 JS의 종류 
 1. Vanilla JS.     [비표준]
 2. ECMA Script  < ECMA [표준]
 3. Type Script < MS

 ECMA Script 5 까지 = Vanilla JS 는 함수형언어
 모든게 함수고 Class같은 개념이 없다 

(  ECMA Script6부터는 Class같은거 있음 )

JS의 변수 만들기

/* let */
/* const */
var = 변수;
끝


문자열 표현 방식
'contents' 
"Contents" 
/* ` contets `*/

Null in JS -> undefined
할당할 데이터가없음 = Null
할당조차 안함 = undefined






















"태그는 굉장히 많습니다. 언제 다 외웁니까. 그냥 구글에 치면 나옵니다." 

first-of-type vs first-child

** Quiz ~
block, inline, inline Blcok의 차이
box Absoult, Static 의 차이

불법강의 -- 

HTML은 어따 쓰나용 
-> 웹 페이지를 만들고 싶을 때 사용합니다 세상 모든 웹사이트는 html을 통해 작성됩니다

HTML Hyper Text Markup Language 
> 다필요없고 Markup Language란 거에 주목하면 됩니다
마크업 언어는 뭔가용 ?
-> 자료의 구조를 표현하기 위한 언어 / 개발언어가 아니라는 뜻입니다. 
웹 페이지에 우리가 글넣고 버튼 넣고 그림넣고 박스넣고 뭐 그런 자료를 입력하는데
그 자료들이 어디에 배치되는지를 기록하기 위해 존재하는 언어가 바로 HTML이라고 보시면 되겠습니다

모든 HTML문서는 .html로 끝나도록 저장하면 되고
기본 코드들을 가지고 있어야 HTML파일로 인식합니다
Head > 헤더부분은 사이트 생성에 필요한 인코딩 형식, 사이트 제목, 필요한 CSS나 JS파일
Body > 바디부분은 실제 웹사이트에서 보여줄 글, 그림을 적습니다. 

HTML은 태그로 작성됩니다
<p> hi </p> 와 같이 태그들을 열고닫고 내부에 글과 그림을 넣습니다.
태그는 종류와 용도가 매우 많습니다.
글을 쓰고 싶을 떄는 글을 쓰는 태그를, 버튼을 쓰고 싶을때는 버튼을 쓰는 태그 사용하면 됨

* 태그를열었으면 </태그명> 으로 닫아줘야 합니다. 안닫아도 되는것도 몇개 있음 <img/> 이런거
** 특정 태그는 안에 href="", src="" 처럼 속성을 넣어 추가정보를 기입가능합니다
*** 용도에 맞는 태그를 사용해야 합니다
 -> 강제사항은 아니지만 HTML은 마크업 언어이기 때문에 자료 구조를 쉽게 파악할 수 있어야 합니다
 따라서 용도에 맞는 태그 사용이 도움이 되고 더욱 '웹 표준'에 맞는 웹을 만들 수 있습니다.

 거의 모든 태그는 style="" 이라는 속성을 열수 있습니다. 
 이 안에 스타일이름 : 스타일값; 형식으로 스타일을 넣을 수 있습니다 
 여러개 넣으려면 뒤에 쭊 쓰면 됩니다

 Style 속성 안에 코드가 매우 길어지면HTMl이 더러워지기 때문에 다른 파일로 뺴서 작업이 가능합니다 
 파일이름은 css겠죠

 css파일은 반드시 사용하기위해서 html에 첨부해야합니다 head 태그 안에 link태그 넣으시고 href로 경로 지정해놓으면 css파일 이용 가능합니다

 css파일에 스타일을 집어넣고 .어쩌구 작명해줄수있습니다 {} 안에 원하는 스타일을 넣으면 작명된이름으로 클래스가 만들어 지게 됩니다 

 html에서 태그에 클래스명을 첨부해서 스타일을 넣을 수 있습니다

클래스 뿐만아니라 아이디 , 태그로도 스타일을 묶을 수 있는데 Css selector라고 합니다 

클래스는 .클래스명{} 으로 적고 class="클래스명" 
아이디는 #아이디명{} 으로 적고 id="아이디명"
태그는 태그명{} 모든 태그명에 대해 스타일을 적용합니다

물론 class, id 를 동시에 가지는 html 요소라면 스타일이 겹칠 수 있는데 그럴 경우 우선순위가 존재하며

style > #id > .class > tag 정확하진 않지만 이런 식입니다. 

태그는 굉장히 많습니다. 언제 다 외우냐고요 ?
-> 뭐하러 외웁니까 구글에치십쇼

h1, div, p 등 태그에 block 속성을 기본적으로 내장하고 있는 얘들이 있습니다. 
해당 속성을 가지고 있으면 그냥 한 행 다 먹습니다
이거 싫으면 display 속성을 다른거 주면 됩니다

일부 속성들은 inherit 됩니다. 부모 태그에 있는게 자식까지 영향준다는 뜻입니다. 

html+css로 사이트 레이아웃 만드는 법 : 
만들고 싶은 레이아웃 디자인 위에 < 네모박스 먼저 그려보고 > 바깥 네모부터 div 이용해서구현해보면 됩니다
어려워 보여도 이렇게하면 알아서 만들 수 있음
근데 그러려면 박스정렬하고 배치하는것도 알아야겠죠
요소들을 띄워서 정렬하는 float 속성들은 근데 요소를 띄우다 보니 다음 오는 요소가 제자리를 못찾아감
(참고 float박스들을 싸매는 하나의 큰 div를 지정하고 폭 지정해주는게 좋다)
안그럼 모바일에서 흘러넘침

float를 썻으면 다음 요소에서 clear를 사용해줍시다 
안넣으면 이상한 레이아웃과 인사합니다 
float:none; 또한 좋습니다.